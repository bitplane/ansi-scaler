[project]
name = "ansi-scaler"
description = "Build reproducible synthetic ANSI-art training corpora"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "accelerate>=1.9",
    "cairosvg>=2.8",
    "chuda-ansi==0.2.3",
    "diffusers>=0.35",
    "einops>=0.8",
    "fastapi>=0.116",
    "jinja2>=3.1",
    "kornia>=0.8",
    "numba>=0.61",
    "numpy>=2.0",
    "pillow>=12.0",
    "protobuf>=6.31",
    "psutil>=7.0",
    "pydantic>=2.11",
    "pyyaml>=6.0",
    "rembg[gpu]>=2.0.69",
    "safetensors>=0.5",
    "sentencepiece>=0.2",
    "timm>=1.0",
    "torch>=2.7",
    "torchvision>=0.22",
    "tqdm>=4.67",
    "transformers>=4.53",
    "typer>=0.16",
    "uvicorn>=0.35",
    "vtracer>=0.6.11",
    "zstandard>=0.25",
]

[project.scripts]
ansi-scaler = "ansi_scaler.cli:app"

[dependency-groups]
dev = [
    "coverage>=7.10",
    "pytest>=8.4",
    "pytest-cov>=6.2",
    "httpx>=0.28",
    "ruff>=0.12",
]

[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core>=3.12,<4"]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]
